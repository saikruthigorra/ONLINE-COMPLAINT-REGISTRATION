{
  "name": "online-complaint-registration-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack MERN complaint registration and management system with role-based dashboards.",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "server": "npm --prefix backend run dev",
    "client": "npm --prefix frontend start",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend start\"",
    "seed": "npm --prefix backend run seed",
    "start": "npm --prefix backend start",
    "build": "npm run install:all && npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}